apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pawcial-backend.fullname" . }}-vault
  namespace: pawcial
  labels:
    {{- include "pawcial-backend.labels" . | nindent 4 }}
type: Opaque
stringData:
  # This should be created manually or via sealed-secrets/external-secrets
  # For now, it's a placeholder - you need to set this value
  VAULT_TOKEN: "REPLACE_WITH_YOUR_VAULT_ROOT_TOKEN"
  VAULT_URL: "https://vault.guven.uk"

