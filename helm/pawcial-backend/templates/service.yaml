apiVersion: v1
kind: Service
metadata:
  name: {{ include "pawcial-backend.fullname" . }}
  namespace: pawcial
  labels:
    {{- include "pawcial-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pawcial-backend.selectorLabels" . | nindent 4 }}

